# Real-World Example

This example demonstrates a real-world application of `jobify` that periodically backs up a database and sends notifications.

## Installation

To get started, first install the dependencies using `uv`:

```bash
uv sync
```

## Running the Application

Once the dependencies are installed, you can run the application with the following command:

```bash
uv run real_world
```
